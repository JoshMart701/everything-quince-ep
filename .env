# Public build-time defaults — override these in Vercel project settings
# These NEXT_PUBLIC_ values are safe to commit; they're exposed to the browser anyway.
# The placeholder values below allow the build to succeed; real values are required at runtime.
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder_anon_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_placeholder
NEXT_PUBLIC_APP_URL=https://everything-quince-ep.vercel.app
